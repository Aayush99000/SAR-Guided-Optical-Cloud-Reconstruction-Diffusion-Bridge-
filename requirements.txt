# ============================================================
# Adaptive Mamba-Bridge (AMB) — Requirements
# Python 3.12 compatible
# ============================================================

# --- Core Deep Learning ---
torch>=2.2.0        # 2.2.0 is the first release with cp312 wheels
torchvision>=0.17.0 # must match torch: 2.2.x <-> 0.17.x
einops>=0.7.0       # tensor rearrangement (used in vision_mamba.py)

# --- Mamba / SSM ---
# mamba-ssm and causal-conv1d require NVCC (CUDA compiler) and only work on Linux + NVIDIA GPU.
# Do NOT install on macOS or CPU-only machines — the project will use the pure-Python SSM fallback.
# On a Linux CUDA machine, uncomment and install these for full hardware-accelerated Mamba kernels:
#
#   pip install causal-conv1d>=1.2.0
#   pip install mamba-ssm>=1.2.0

# --- Geospatial / Remote Sensing ---
# rasterio wheels on macOS already bundle their own GDAL — no separate GDAL pip install needed.
# If you get a gdal-config error, install the system library first via Homebrew:
#   brew install gdal
# Then re-run pip install -r requirements.txt
rasterio>=1.3.9     # bundles GDAL internally on macOS/Windows pip wheels

# --- Numerical ---
numpy>=1.26.0       # cp312 wheels from 1.26.0

# --- Data Augmentation ---
albumentations>=1.4.0  # satellite image augmentation (flips, rotations, elastic)

# --- Dataset (Sen12MS-CR uses HDF5 format) ---
h5py>=3.10.0        # read .h5 patch files from Sen12MS-CR

# --- Experiment Tracking ---
tensorboard>=2.15.0 # loss curves, image grids, metric logging
wandb>=0.16.0       # optional: Weights & Biases experiment tracking

# --- Progress & Utilities ---
tqdm>=4.66.0        # progress bars for training/inference loops

# --- Visualization ---
matplotlib>=3.8.0   # plot reconstructed images, metric curves
Pillow>=10.0.0      # image I/O for saving PNG previews

# --- Metrics ---
torchmetrics>=1.3.0 # PSNR, SSIM, SAM implementations with GPU support

# --- Segmentation (Frozen Judge / Tier 2 Validation) ---
segmentation-models-pytorch>=0.3.3  # pre-trained land cover segmentation models
