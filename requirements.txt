# Python 3.12 compatible requirements
# See compatibility notes below

torch>=2.2.0       # 2.2.0 is the first release with cp312 wheels (2.1.x has none)
torchvision>=0.17.0  # 0.17.0 is the first release with cp312 wheels (0.16.x has none)
einops>=0.7.0      # Pure-Python package; 0.7.0 works on 3.12 (no compiled extensions)
rasterio>=1.3.9    # 1.3.9 is the first 1.3.x release with cp312 wheels
numpy>=1.26.0      # 1.26.0 is the first numpy release with cp312 wheels (1.24/1.25 have none)
